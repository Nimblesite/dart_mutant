<!-- agent-pmo:5547fd2 -->
# Agent Instructions

All project rules, coding standards, hard constraints, build commands, and
architecture notes live in [`CLAUDE.md`](CLAUDE.md).

Read that file in full before writing any code. Do NOT add rules here —
keep everything in `CLAUDE.md` so there is exactly one set of instructions
to maintain.
